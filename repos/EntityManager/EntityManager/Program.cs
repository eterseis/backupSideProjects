
using Swed64;
using System.Text;

namespace EntityManager
{
    internal class Program

    {
        static void Main(string[] args)
        {
            //Swed swed = new Swed("ac_client");
            //var moduleBase = swed.GetModuleBase(".exe");

            //var aCEntityManager = new ACEntityManager(swed, moduleBase);

            //while (true)
            //{
            //    Console.Clear();
            //    aCEntityManager.UpdateEntities();
            //    aCEntityManager.UpdateLocalPlayer();
            //    foreach (var entity in aCEntityManager.GetEntities())
            //    {
            //        Console.WriteLine($"{entity.Name} :: {entity.Health}");
            //    }
            //    Console.WriteLine("\n" + aCEntityManager.GetLocalPlayer().Name + " :: " + aCEntityManager.GetLocalPlayer().Health);
            //    Thread.Sleep(500);
            //}

        }
    }
}

